# Keep disabled until the first platform administrator and store membership
# documents have been provisioned and verified.
NEXT_PUBLIC_ENTERPRISE_AUTH=false

# Firebase service account JSON encoded as base64. Required by the secure
# server-side user administration API. Never expose this as NEXT_PUBLIC_*.
FIREBASE_SERVICE_ACCOUNT_KEY_BASE64=
